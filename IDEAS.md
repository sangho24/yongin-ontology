# UI 인터랙션 아이디어 — 숫자 컴포넌트 설계

> 핵심 철학: 숫자(KPI·매출·비용)는 정적 텍스트가 아니라 **출처를 추적하고 시뮬레이션할 수 있는 객체**로 다룬다.
> 온톨로지 관점에서 보면 각 숫자는 A-Box 인스턴스이며, 그 lineage(어떤 데이터·로직을 거쳐 산출됐는지)와 변동성(driver에 따라 어떻게 바뀌는지)을 UI에서 노출하는 것이 목적.

---

## 1. 시각적 마킹 — "이 숫자는 변경/탐색 가능합니다"

정적 라벨과 동적 숫자를 시각적으로 분리해서, 사용자가 *"여기는 인터랙션 가능"* 임을 즉시 인지하게 한다.

- 숫자 영역을 **박스/카드로 한 번 더 감싸기** (border, 배경 톤 차이, 그림자)
- hover 가능 시그니처
  - 점선 underline 또는 dotted border
  - hover 시 미세한 색 변화 / cursor 변경
- 정적 텍스트(`총`, `원`, `명`) vs 동적 숫자 본체 분리
- 단위·통화는 숫자 박스 바깥에 두기 → 박스 안의 값만 변동성 영역임을 강조

> ⚠ 모든 숫자에 박스를 두르면 시각적 노이즈가 커짐 → **인터랙티브한 숫자**(driver와 연결된 KPI)에만 적용. 단순 표기 숫자는 일반 텍스트.

---

## 2. Hover — 기본 인터랙션 (가벼운 정보)

마우스 올리면 즉시 tooltip. 깊은 정보는 클릭으로 미루고 여기서는 핵심 메타만.

표시 내용:
- 출처 (어느 시트·어느 계정의 합계인지)
- 산식 요약 한 줄 (예: `SUMIFS(원장, 계정=40400, 구역=세수연)`)
- 기간·단위
- 마지막 갱신 시점 (선택)

원칙:
- 2초 이내에 읽을 수 있는 분량
- 깊게 들어가고 싶으면 "자세히 보기" 링크/버튼

---

## 3. Click — 계산 로직 상세 (Lineage 패널)

숫자 본체 또는 옆 아이콘 버튼 클릭 시, **사이드 패널 또는 모달**로 lineage를 펼친다.

표시 내용:
- **원천 데이터 출처**
  - 어떤 raw 시트 (예: `용인공원_계정별원장.xls`)
  - 어떤 행 범위 / 필터 조건
- **적용된 로직 단계별 표시**
  1. raw row 추출
  2. 마감분개·이월 제외 패턴
  3. 구역 매핑 적용
  4. SUMIFS 집계
  5. 배부 driver 적용 (있다면)
- **관련 적요 패턴** — 어떤 키워드로 분류됐는지 (예: 카드비 H/I 패턴, 기타구역 재분류 키워드)
- **신뢰도/검증 상태** — "원장과 0원 차이 검증 완료" 같은 메타

> 온톨로지 관점에서는 이 패널이 곧 해당 인스턴스의 **생성 lineage 그래프**다. T-Box 클래스 정의와 A-Box 인스턴스의 source provenance를 같이 보여줄 수 있으면 가장 강력함.

---

## 4. What-if 슬라이더 — 시뮬레이션

숫자가 어떤 driver의 함수인지를 인터랙티브하게 보여주는 핵심 기능.

기본 흐름:
- 핵심 driver 변수를 슬라이더로 노출
  - 예: 회원 수 +n, 분양 건수, 채널 비중, 평균 단가, 가입률
- 슬라이더 조작 시 연결된 KPI/매출이 실시간으로 재계산되어 변동
- 변동된 숫자는 시각적으로 강조 (색상 변경, ▲/▼ delta, 원래 값 회색 표기)

확장:
- 시나리오 비교 (현 시점 vs +10% 시나리오 동시 표기)
- 시나리오 저장 / 즐겨찾기
- 슬라이더로 역질의 가능 (예: "매출 +10%를 만들려면 회원 몇 명 증가가 필요한가" — 슬라이더를 KPI 쪽에 걸고 driver 쪽이 따라 움직이는 inverse 모드)

> 이 기능이 회사 입장의 LTV·ROI·CAC 같은 질문에 답할 수 있는지를 시연하는 가장 강한 데모 포인트가 됨.

---

## 5. 추세선 / 시계열

시간 축이 있는 숫자는 작은 sparkline을 숫자 박스 안에 inline으로 표시.

- 숫자 옆에 mini sparkline (월별 또는 분기별)
- 클릭하면 본격 차트로 확장 (recharts)
- 추세선 + 이상치 마킹 (선택)
- 슬라이더 시뮬레이션과 연동 → 시뮬레이션 결과가 시계열에 어떻게 반영되는지 표시

---

## 우선순위 (구현 난이도 vs 임팩트)

| 단계 | 항목 | 난이도 | 임팩트 |
|------|------|--------|--------|
| 1 | 시각적 마킹 + Hover tooltip | 낮음 | 중 — 즉시 인터랙티브 느낌 부여 |
| 2 | Click → Lineage 패널 | 중 — 데이터 lineage 메타 정의 필요 | 높음 — 온톨로지 시연의 본질 |
| 3 | Sparkline | 낮음 — recharts 이미 설치됨 | 중 |
| 4 | What-if 슬라이더 | 높음 — driver 모델 정의 필요 | 매우 높음 — KPI 질문에 답하는 데모 |

---

## 기술 메모

- `recharts ^3.8.1` 설치됨 → sparkline·시계열 차트 바로 사용 가능
- `@xyflow/react ^12.10.2` 설치됨 → T-Box 시각화에 사용 중. lineage 패널의 그래프 표현에도 재활용 가능
- `zustand ^5.0.13` → 시뮬레이션 상태(슬라이더 값, 시나리오) 전역 관리 적합
- 숫자 컴포넌트 추상화 제안

  ```tsx
  <NumberCell
    value={62_850_511_923}
    unit="원"
    lineage={{
      source: '계정별원장 40400',
      formula: 'SUMIFS(원장, 계정=40400, 구역=세수연)',
      verified: true,
    }}
    drivers={['memberCount', 'avgPrice']}
    timeseries={monthlyData}
  />
  ```

  이 컴포넌트가 hover/click/슬라이더 연동/sparkline을 모두 통합하는 단일 진입점이 되도록 설계.

---

## 데모 시나리오 — 회의용 스토리라인

회의에서 시연한다고 가정했을 때 권장 흐름:

1. **landing 페이지** → 매출 KPI 박스에 mouse over → "이 숫자는 어디서 왔지?" hover tooltip 표시
2. **숫자 클릭** → 사이드 패널 열림 → raw 원장 → 마감분개 제외 → 구역 매핑 → SUMIFS 까지의 lineage가 단계별로 펼쳐짐
3. **슬라이더 조작** → 회원 수 +20% 끌어올리면 KPI 박스 숫자가 실시간으로 변경되고 ▲ delta 표시
4. **추세선 클릭** → 월별 추이 차트로 확장, 시뮬레이션이 어떻게 반영되는지 시계열로 보여줌

이 흐름이 자연스럽게 흐르면, 회의 안건이었던 *"어디까지 답할 수 있는 온톨로지인가"* 의 답을 UI 자체로 보여주게 됨.

---

## 풀어야 할 결정 사항 (내일 작업 시작 전 정리)

- [ ] `<NumberCell>` 컴포넌트 인터페이스 확정 (lineage·driver·timeseries 형태)
- [ ] 우선 시연할 KPI 3~4개 선정 (모든 숫자가 아니라 임팩트 있는 것에 집중)
- [ ] Driver 모델 — 어떤 입력 변수가 어떤 KPI에 어떻게 영향을 주는지 (간단한 선형 모델로 시작)
- [ ] Lineage 메타 데이터 스키마 — 정적 JSON으로 시작할지, 별도 lineage store 둘지
- [ ] Hover/Click 인터랙션의 디자인 토큰 (border, 색상, 모션 duration)
