@AGENTS.md

# 대시보드 포지셔닝 (필독 — 모든 작업 결정의 기준)

이 대시보드는 **회사보고용** 산출물입니다. 내부 팀 회의용·정렬 도구가 아닙니다.

## 1차 사용 목적
- **CEO·부서장이 실제로 매일 켜서 쓰는 운영 도구** — 보고서 PDF가 아니라 살아있는 BI 툴
- 임원 보고는 이 BI를 화면 공유하면 그 자체로 이루어지는 형태 (별도 "보고 화면" 만들지 않음)
- 분석 결과를 보여주는 곳이 아니라 **활동원가·구역별 손익을 일상적으로 탐색·드릴다운하는 곳**
- 재무팀 사용자에게는 ERP 이동·활동 자동 등록 같은 **운영 액션 버튼**까지 제공

## 2차 사용 목적
- **PwC AX/DX 컨설팅 reference 사례**로 보존
- 다른 프로젝트에서 design system·컴포넌트·방법론을 복제 사용
- 외부에 노출 가능한 수준의 완성도가 필수

## 핵심 원칙

1. **퀄리티 최우선** — 컨설팅 deliverable급 시각·인터랙션 완성도. 와이어프레임·프로토타입 톤 금지
2. **미합의·미완성 흔적 노출 금지** — "⚪ 합의 필요", "🔴 데이터 부족" 같은 회의용 라벨링 사용 금지. 분석 결과는 통합된 결론으로 제시
3. **인사이트 우선, 데이터·로직은 lineage 패널에 격납** — 의사결정자가 30초 안에 핵심을 흡수할 수 있는 hero 구조
4. **온톨로지·RFI·Knowledge Graph는 "방법론적 차별화"로 포지셔닝** — 미완성 process가 아니라 거버넌스 framework로 노출
5. **재사용 가능한 design system** — 모든 컴포넌트는 design token·prop 기반으로 추상화하여 다른 컨설팅 프로젝트에서 복제 가능하게 설계

## 작업 시 자주 빠지는 함정 (피하기)
- 데이터 갭을 그대로 사용자에게 노출 → ❌ 분석가가 처리한 결과만 보여주고, 갭 처리 방법론은 별도 섹션에서 우아하게 시연
- 회의 안건 도출용 매트릭스·체크리스트 → ❌ 보고용에는 결론과 제언만
- 디자인을 기능보다 후순위 → ❌ 컨설팅 reference로 남기려면 디자인이 곧 기능
- 정적 보고서 톤 (스크롤 후 끝) → ❌ CEO가 클릭·드릴다운으로 탐색하는 살아있는 도구 톤
- 정보를 한 페이지에 다 욱여넣기 → ❌ 한눈에 핵심 / 클릭하면 깊이로 단계 분리

# 정보 구조 (IA)

## 주요 뷰 (사이드바 1차 메뉴)
엑셀 시트 비유로 "Overview / 상조 VC / 장지 VC" 세 개의 큰 뷰가 사용자의 주 작업 공간.

- **Overview** — 그룹 전체 현황. 핵심 KPI, 기간별 추세, 두 VC 비교
- **상조 VC** — 라이프 / 회원 master 중심 Value Chain
- **장지 VC** — 용인공원·YPL / 객체(구역) master 중심 Value Chain
- **Root Cause** — 원인 분해 (이슈가 발견됐을 때 들어가는 분석 페이지)
- **Data Model** (META) — T-Box·RFI 거버넌스 framework (방법론적 차별화)

## 각 뷰 내부 정보 계층 (위에서 아래로 깊어짐)
1. **기본 재무 정보** — PPT에 실린 매출·비용·이익 핵심 표·차트
2. **Root cause 요약** — 이 뷰에서 가장 임팩트 있는 원인 분해 1~2개
3. **활동원가 탐색기** — 구역 선택 → 월/연 매출·비용 → 항목별 → 추출 로직 (IDEAS.md NumberCell 적용)
4. **부서별 KPI 드릴다운** — 활동·구역과 연결된 부서 단위 KPI

## 활동원가 탐색 동선 (핵심 인터랙션)
구역 드롭다운(아너스톤 특정 구역, 정담원 특정 구역 등)
→ 선택 시 매출·비용·항목 패널이 애니메이션으로 펼쳐짐 (월/연 토글)
→ 항목 클릭 시 NumberCell 인터랙션 (lineage·산식·sparkline)
→ 재무팀 사용자에게는 동일 패널 안에 액션 버튼 노출
   - "ERP로 이동" — 해당 분개 ERP 화면 직접 열기
   - "활동 자동 등록" — 누락된 활동을 RFI·Data Model에 자동 추가

## 디자인 톤 & 톤 레퍼런스
- 색상: 어제 잡힌 용인공원 색상 시스템 그대로 사용 (`globals.css`의 `--teal` 계열, warm off-white `#fafaf7`, Pretendard)
- 컴포넌트 간 간격: **미니멀** — 답답하지 않은 정도의 호흡
- 사이드바·상하단 메뉴 디자인 레퍼런스: https://dribbble.com/shots/26628350-Shopeers-AI-Powered-B2B-eCommerce-Analytics-Dashboard
  - 좌측 고정 사이드바, 카테고리 라벨링, hover state, 컴팩트한 nav item, top bar에 period·user·검색
- 모션: 활동원가 드릴다운 시 펼침 애니메이션 (200~300ms ease-out), 절제된 톤

## 작업 모드
- 정보가 한 화면에 다 보이는 디자인 < 핵심만 보이고 클릭으로 깊어지는 디자인
- 각 뷰가 "최종 보고서에 들어갈 deliverable의 일부"를 포괄하되, 그 형태는 보고서 슬라이드가 아니라 인터랙티브 BI 컴포넌트

## UI 텍스트 — 보고서 톤 엄격 금지 ⚠

**절대 하지 말 것 (발견하면 즉시 삭제):**
- 페이지 하단에 "READING THE GRAPH", "HOW TO READ", "GUIDE", "범례 설명", "그래프 읽는 법" 같은 별도 설명 섹션 추가
- `<section>` / `<div>` 블록으로 차트·그래프·테이블 사용법을 단락 형태로 설명하는 텍스트
- 여러 bullet이나 단락으로 구성된 "분석 방법론 안내" 블록
- 차트 아래에 "이 그래프는 ~를 나타냅니다" 같은 캡션형 해설

**허용:**
- AppLayout `narration` prop의 NOTES aside — 인사이트·비즈니스 맥락 (방법론 설명 X)
- 범례(legend) — 색상·기호 의미만, 한 줄씩
- 하단 footer 한 줄 상태 표시 (`현재 layer: Instance · Network` 등)
- 노드/행 클릭 시 열리는 detail 패널 — 해당 인스턴스의 속성·상태

**원칙**: CEO가 화면을 켰을 때 "설명문"이 눈에 들어오면 안 됨. 데이터가 스스로 말해야 함. 꼭 필요한 컨텍스트는 AppLayout narration(우측 aside)에만.
